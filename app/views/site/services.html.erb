<section id="services">
   <div class="container">
     <div class="row">
      	<h2>Servicios</h2>
      	<% @services.each_with_index do |service, index| %>
      	<article id="servicio-<%= index %>">
      		<h3><%= service.title %></h3>
      		<p><%= service.header %>
      		<p><%= service.body.html_safe %>
      	</article>
      	<% end %>
      </div>
   </div>
</section>